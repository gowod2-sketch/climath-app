# climath-app

이 저장소에는 두 프로젝트가 함께 있습니다.

| 경로 | 프로젝트 |
|------|---------|
| `/` | climath-oap (React + Vite + Capacitor + Supabase) |
| `/mathuit` | Mathuit — 수학 팁개념 학습 PWA (Netlify 배포 대상). 처음엔 수열의 극한 전용이었으나 지수·로그함수의 미분(단원 II), 삼각함수의 덧셈정리(단원 III)가 이어 붙었고 공통수학1(단원 IV~)로 계속 확장 중이다 |

## 하네스: mathuit 수학 콘텐츠 파이프라인

**목표:** `docs/*.md` 수학 명세를 `mathuit/content/{concept,tip}/*.md` 앱 콘텐츠로 변환하고 `build.py` 게이트를 통과시킨다.

**트리거:** mathuit 콘텐츠 변환·추가·수정 작업 요청 시 `mathuit-content-pipeline` 스킬을 사용하라. 콘텐츠에 대한 단순 질문은 직접 응답해도 된다.

**실행 모드:** 서브 에이전트. `Agent` 도구로 직접 호출하고 반환값과 `_workspace/` 파일로 결과를 모은다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-09-04 | 초기 구성 (에이전트 5, 스킬 4) | 전체 | — |
| 2026-09-05 | 팁 판정 4단계화 · 결정 권한 경계 | spec-analyst | 실전 투입 시 비표준 판정값 창작 |
| 2026-09-05 | 본문/수식 변환 차이표 · 문단 규칙 실측 정정 | content-format | 문서가 build.py 실제 동작과 어긋남 |
| 2026-09-05 | curve 카탈로그 전면 재검산 | tip-graph | 행 이름 2개가 뒤바뀌어 있었음 |
| 2026-09-05 | 표기 가능성 사전 점검 · 표현불가 전량 게이트 | spec-analyst, 파이프라인 | docs/04에서 팁 0개 단원 발견 |
| 2026-09-05 | 유도 서술 축 · 엄밀성 경계 · 규약 대조 | math-reviewer, build-gate | 무주공산 항목 발견 |
| 2026-09-05 | 재호출 지침 5건 신설 | 에이전트 전체 | 부분 재실행 경로에 안내가 없었음 |
| 2026-09-05 | 근거 사슬 보류 · 중첩 사각지대 점검 | build-gate, 파이프라인 | 보류가 남긴 순환 서술 발견 |
| 2026-09-05 | 정합성 감사 반영 (높음 6건) | 전체 | 7회 개정 누적 모순 |
| 2026-09-05 | `## graph` 선택화 · 공통수학1로 전환 | build.py, index.html, tip-graph, content-format, build-gate | 공통수학1 대부분 용어가 수열 산점도로 표현 안 됨 |
| 2026-09-05 | docs/03·04 완성·배포 (unit II·III, 개념 44개·팁 34개) · docs/05(다항식) 착수 | mathuit/content, ship.py | 그래프 선택화로 표현불가 판정이 대부분 해소되어 실제 착수 가능해짐 |

## 무엇을 언제 할지 — 순서 규칙

R&D를 "지금 크기에서 한 번, 키워서 또 한 번" 반복하지 않는다. 기준은 크기가 아니라 **비용이 크기에 비례하는가**다.

| | 예 | 언제 |
|---|---|---|
| **크기에 비용이 붙는 것** | 스키마·식별자·번호체계, 목차 구조, 사용자 데이터 저장 키 | **다음 대량 생산 전에** 끝낸다. 개념 하나 늘 때마다 되돌리는 비용이 같이 는다 |
| **크기와 무관한 것** | 렌더러 추가, 팝업 레이아웃, 퀴즈 체계, 연출 개선 | 아무 때나. 59개일 때 고치나 200개일 때 고치나 같다 |

**실제 사례 (2026-09-05).** 필기(`INK`)와 마지막 위치가 페이지 **인덱스**로 localStorage 에 저장돼 있었다. 인덱스는 콘텐츠 순서의 함수라, 단원을 끼워 넣거나 순서를 고치면 기존 사용자의 필기가 다른 개념 위로 조용히 옮겨 간다. 개념 59개 시점에 잡아 개념 id 키로 바꿨다 — 200개까지 갔으면 마이그레이션 대상만 3배가 됐다.

**마이그레이션은 "그때의 순서"를 스스로 들고 있어야 한다.** 처음에는 옛 인덱스를 *현재* 순서로 풀었는데, 그러면 재배치할 때마다 매핑이 어긋나 "키 전환 배포 → 그다음 재배치"라는 2단계 제약에 묶인다. `index.html` 의 `INK_LEGACY_ORDER` 는 배포본 순서(개념 44개 + 하드코딩 퀴즈 1칸)를 고정해 둔 목록이고 **과거를 가리키므로 앞으로도 바꾸지 않는다.** 덕분에 콘텐츠를 어떻게 재배치하든 옛 필기는 같은 개념에 남는다 — 실제로 순서를 완전히 뒤집어 검증했다(고정 목록: c03·c14·c44 유지 / 현재 순서 해석: c56·c45·c15 로 오배치).

## 알려진 제약

팁개념 그래프(`## graph`)를 쓴다면 **n=1..12 수열 산점도로 고정**입니다. 수열의 극한 단원(`concept/c01`~`c11`)에는 맞지만, `docs/03_지수로그함수_미분.md`·`docs/04_삼각함수_덧셈정리.md`·공통수학1의 용어 상당수는 수열로 표현되지 않습니다.

**2026-09-05부로 `## graph`는 선택입니다.** 산점도로 안 되는 개념은 그래프를 지어내지 말고 블록을 생략하면 `## steps`(스토리보드 캡션)만으로 렌더됩니다. `build.py`/`index.html` 모두 대응됨 — 자세한 판단 기준은 `.claude/skills/mathuit-tip-graph/SKILL.md`의 "산점도로 표현할 수 없는 용어" 참조. 스텝 캡션조차 못 쪼개는 소수 용어만 에스컬레이션 대상.

**진행 현황 (2026-09-05):** `docs/03`(지수·로그함수의 미분, unit II)·`docs/04`(삼각함수의 덧셈정리, unit III) 완성·배포 완료. **우선순위는 공통수학1**(다항식·방정식과 부등식·경우의 수·행렬) — `docs/05_다항식.md`부터 명세를 새로 작성해 착수했다(unit IV~). 공통수학1은 기존 docs/02~04와 달리 원본 명세가 없어서, 파이프라인(spec-analyst) 실행 전에 매번 명세부터 새로 써야 한다 — docs/05가 그 첫 사례이자 형식 선례다.
