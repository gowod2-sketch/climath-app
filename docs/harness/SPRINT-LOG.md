# 하네스 스프린트 로그

mathuit 콘텐츠 파이프라인 하네스를 반복 개선한 기록입니다.
회차마다 **무엇을 바꿨고 왜 바꿨는지**를 남깁니다. 결과만 적으면 다음 사람이 같은 판단을 반복합니다.

- 스프린트 구간: 2026-09-05 01:20 ~ 06:00 KST
- 대상 하네스: `.claude/agents/` 5개 · `.claude/skills/` 4개
- 검증 소재: `docs/03_지수로그함수_미분.md` → `mathuit/content/`
- 절대 합격선: `cd mathuit && python3 build.py`

## 회차 요약

> 시각은 커밋 시각(KST) 기준입니다. 회차 0~5가 **31분**(01:27~01:58)에 끝났습니다 — 서브 에이전트가 병렬로 돌아 벽시계 시간이 짧습니다.

| 회차 | 시각(KST) | 한 일 | 발견 | 하네스 변경 |
|------|-----------|-------|------|------------|
| 0 | ~01:27 | 하네스 최초 구축 (설계 전용) | 팀 API 부재 · 그래프 형식 제약 · c12 선행 | 초기 생성 |
| 1 | 01:36 | `docs/03` 실전 투입 (Phase 2) | 게이트 작동 확인 · 에스컬레이션 6건 중 **4건이 하네스 결함** | 판정 4단계화 · 권한 경계 · 게이트 세분화 |
| 2 | 01:43 | `concept-author` 투입 (c13/c15/c16) | 차단 게이트 정확 작동 · **스킬 문서 사실오류 5건** | 본문/수식 변환 차이표 · 문단 규칙 정정 · 역할 경계 |
| 3 | 01:46 | `tip-author` 투입 (`euler`) | 검산 규칙이 상류 오류 포착 · **카탈로그 행 이름 2개 뒤바뀜** | curve 카탈로그 전면 정정 · 부가규칙 4건 |
| 4 | 01:52 | `docs/04` 프로브 (분석 전용) | 과적합 없음 · **개념 22개 전부 팁 0개** · θ 하드 블로커 | 표기 가능성 사전 점검 · 표현불가 전량 게이트 |
| 5 | 01:58 | `math-reviewer` 투입 (마지막 미검증) | **수학 오류 2건 포착** · 무주공산 축 3개 | 유도 서술 축 · 엄밀성 경계 · 규약 대조 |
| 6 | 02:40 | 부분 재실행 검증 | **에이전트 5개 전부 재호출 지침 누락** | 재호출 지침 5건 신설 · 검증 통과 |
| 7 | 03:05 | `build-gate` 규약 대조 검증 | **근거 사슬 끊김 2건** · 중첩 사각지대 · 신호 노이즈 63% | 규약 대조 절 전면 개정 · 근거 사슬 보류 |
| 8 | 03:40 | 하네스 내부 정합성 감사 | 높음 6 · **라이브 앱 버그 2건** | 높음 전건 수정 · 앱 버그 별도 보고서 |

---

## 회차 0 — 구축 (~01:27)

**한 일**
- `revfactory/harness` v1.2.0 감사 후 `~/.claude/skills/`에 설치
- 에이전트 5개 · 스킬 4개 생성, `claude/harness-configuration-g8dd7n` 에 커밋 (`90999bd`)

**발견**

1. **에이전트 팀 API가 이 환경에 없다.** `TeamCreate`가 도구 목록에 부재. Harness의 기본 실행 모드가 팀인데, 없으면 **에러 없이 단일 에이전트로 폴백**한다(저자 문서의 "silently breaks"). 6개 패턴 중 4개가 조용히 깨진다. → 처음부터 서브 에이전트 모드로 설계.

2. **팁 그래프가 수열 산점도로 고정.** `n=1..12` 산점도 외 형식이 없다. `c01~c12`(수열의 극한)에는 맞지만 `docs/03`(미분)·`docs/04`(삼각함수)는 용어 상당수가 수열이 아니다. 그런데 **`build.py`는 이걸 못 잡는다** — 형식만 검사하므로 아무 curve나 넣어도 통과한다. → 표현 불가 시 에스컬레이션 규칙을 `tip-author`와 `mathuit-tip-graph`에 명문화.

3. **`c12`가 이미 단원 II를 시작해 놓았다.** `unit: II. 지수함수와 로그함수의 미분`. `docs/03`은 신규 단원이 아니라 진행 중 단원의 연속이고 order는 13부터다. 기존 자산을 안 읽었으면 단원을 중복 생성했을 것이다.

**하네스 변경**: 초기 생성 (변경 이력 없음)

**미해결**: 파이프라인 실제 실행 검증 안 됨 — 회차 1에서 수행


---

## 회차 1 — 첫 실전 투입 (01:27~01:36)

**한 일**: `spec-analyst`를 `docs/03_지수로그함수_미분.md`에 실제로 투입 (opus, 74k 토큰, 3분 29초)

**결과**: 개념 9개(c13~c21) · 신규 팁 1개(`euler`) · 재사용 5개 · **표현불가 4개** · 에스컬레이션 6건

**검증된 것 — 회차 0의 두 예측이 실제로 맞았다**

1. **표현불가 에스컬레이션이 실제로 걸렸다.** `derivative`, `naturallog`, `chainrule`, `basechange` 4개가 산점도로 표현 불가 판정되어 파일이 생성되지 않았다. 특히 `naturallog`에 대한 에이전트의 근거가 정확하다 — *"ln n을 억지로 찍으면 발산하는 수열 그림이 되어 학생이 자연로그를 발산 개념으로 오각인한다."* **`build.py`는 이걸 절대 못 잡는다.** 형식만 보기 때문이다. 하네스가 실제로 값을 만든 지점.
2. **기존 자산 실독 규칙이 작동했다.** `c12`가 이미 `unit II`를 시작해 놓은 것을 발견해 order 13부터 이어갔다. 안 읽었으면 단원을 중복 생성했다.

**발견된 하네스 결함 4건 — 에스컬레이션 6건을 역추적한 결과**

| 에스컬레이션 | 원인 | 결함 여부 |
|---|---|---|
| 2, 3 (기존 팁 oneline 확장) | 판정이 `재사용/신규/표현불가` 3개뿐 | **결함** — 에이전트가 `재사용(조건부)`라는 비표준 값을 지어냈다. 선택지가 없으면 만들어낸다 |
| 1 (교육과정 밖 내용) | 범위 판단 규칙 부재 | **결함** — 에이전트가 스스로 기준을 세워 처리했다 |
| 5 (신규 sec 승인) | unit/sec 신설 권한 미정의 | **결함** |
| 6 (퀴즈 개념화) | quiz 생성 기준 부재 | **결함** |
| 4 (표현불가 처리) | — | 설계대로 작동 |

**하네스 변경**

- `spec-analyst`: 팁 판정을 **4단계**로 (`재사용 / 기존 확장 / 신규 / 표현불가`). "조건이 붙으면 그것은 기존 확장이다"를 명문화 — 중간값 창작 방지
- `spec-analyst`: **결정 권한 경계표** 추가 (unit/sec 신설, 기존 팁 수정, 교육과정 경계는 사람 승인)
- `spec-analyst`: 교육과정 초과 시 **제외 + 보고** 규칙 (임의 삭제 금지)
- `spec-analyst`: 퀴즈 개념 기본값을 **만들지 않음**으로 (오답 선택지 창작 비용이 개념 본문보다 크다)
- `spec-analyst`: 표현불가 판정에 **영향도(핵심/주변)** 필드 추가 — 형식 확장 판단 근거
- 오케스트레이터: 게이트를 **유형별 차단 범위**로 세분화. 전부 멈추면 승인 불필요한 작업까지 선다

**핵심 교훈**: 에이전트에게 선택지를 3개만 주면 4번째 상황에서 **비표준 값을 창작한다.** 판정 체계는 실제 발생 가능한 경우를 전부 덮어야 한다. 에스컬레이션을 역추적하면 이런 누락이 드러난다.

**미해결(사람 결정 대기)**: 에스컬레이션 6건 — 특히 #4(그래프 형식 확장 여부)는 `docs/04` 삼각함수 단원에서 더 크게 걸리므로 착수 전 확정 권장


---

## 회차 2 — 개념 작성 및 문서 사실오류 발견 (01:36~01:43)

**한 일**: `concept-author`를 c13/c15/c16에 투입 (opus, 69k 토큰, 2분 28초)

**검증된 것 — 회차 1에서 만든 게이트가 정확히 작동**

요청대로 **차단되지 않은 3개만** 만들고 c14(팁 확장 대기)·c17~c21(sec 신설 대기)은 건드리지 않았다. order 14를 비워 둔 것도 지시대로다. `build.py`는 `euler` 링크 미해결로만 실패했고, 에이전트가 스크래치패드에 사본을 만들어 스텁으로 확인한 결과 **그 외 에러는 없었다**(저장소는 건드리지 않음). 게이트 세분화가 없었으면 9개 전부 멈춰 있었을 것이다.

**발견 — 내 스킬 문서에 사실 오류 5건**

에이전트에게 "규칙이 부족하거나 모호했던 지점을 솔직히 적으라"고 요구한 것에서 전부 나왔다. `build.py` 소스로 **5건 모두 재확인**했다.

| 스킬에 적었던 것 | 실제 | 근거 |
|---|---|---|
| "`lim`/`log`는 자동 로만체" | **`$$` 안에서만**. 본문 `ln a`는 `l`만 이탤릭 | `prose()`가 `\b([A-Za-z])` 단일 문자만 매치 |
| "지수는 `r^n`" | 본문에선 캐럿이 문자 그대로 | `prose()` 정규식이 `_` 첨자만 캡처 |
| "첫 에러에서 멈춘다" | front matter·수식만 즉시 종료. **링크는 누적 후 일괄 보고** | `missing` 리스트에 모아 한 번에 출력 |
| "문단 각 2~4문장" | 기존 c01·c09·c11·c12 **전부 p1이 1문장** | 실측 |
| (누락) | front matter `title`은 변환 안 됨 | `parse_concept`이 원문 반환 |

실제로 c15/c16 title에 `e^x`·`a^x`가 들어가 캐럿이 그대로 노출될 뻔했다. 코퍼스 관례(우리말 제목)에 맞춰 수정했다.

**추가 발견 — 역할 경계 침범**

`spec-analyst`가 인벤토리에 팁의 `curve` 식까지 제안했는데, 이건 `tip-author`의 영역이고 **방향이 틀렸다**(제안한 식은 화면에서 점이 내려가는데, `(1+1/n)^n`은 e로 증가하며 수렴). 상류 제안은 하류에게 근거 없는 앵커로 작동하고, 두 단계를 거쳤으니 검토된 것처럼 보인다 — 실제로는 아무도 안 본 것이다.

**하네스 변경**
- `mathuit-content-format`: **"본문과 수식은 변환 규칙이 다르다"** 대조표 신설 (오류 4건의 뿌리)
- `mathuit-content-format`: 문단 분량을 실측 기반(p1 1~2, p2 2~3)으로 정정, "분량 규칙보다 코퍼스 일관성 우선" 명시
- `mathuit-build-gate`: 종료 방식을 검사 종류별로 구분
- `concept-author`: 표현불가 용어에 뜻을 한 구절 덧붙이는 규칙 (팝업이 없으니 확인할 데가 없다)
- `spec-analyst`: **하류 영역 침범 금지** 절 신설

**핵심 교훈**: 하네스 문서의 사실 오류는 **리뷰로 안 걸린다.** 그럴듯하게 읽히기 때문이다. 실제로 파일을 만들어 보는 에이전트만 발견한다. 그래서 에이전트 프롬프트에 "규칙이 모호했던 지점을 보고하라"를 반드시 넣어야 한다 — 이번 개선의 대부분이 그 항목에서 나왔다.


---

## 회차 3 — 팁 그래프 설계, 그리고 카탈로그 자체가 틀렸음을 발견 (01:43~01:46)

**한 일**: `tip-author`를 `euler`에 투입 (opus, 73k 토큰, 2분 13초). `build.py` **1회 통과**, 재실행 없음.

**검증된 것 — 검산 규칙이 상류 오류를 실제로 잡았다**

`spec-analyst`가 인벤토리에 `150 - 90/n` 계열을 제안했는데, `tip-author`가 대입해 보고 **거부**했다:

> 대입하면 y 60 → 142.5, 즉 화면에서 **아래로** 내려갑니다. 반전 좌표계에서 이건 수학값이 *감소*하며 e로 접근한다는 그림이라, `(1+1/n)^n`이 위에서 e로 내려온다고 학생이 잘못 배웁니다. 실제로는 증가 수열이므로 제안을 버렸습니다.

대신 실제 수열값의 1차 변환 `362.8 - 111.4*Math.pow(1+1/n, n)`을 쓰고 `new Function`으로 평가해 검산까지 했다. 근사 모양이 아니라 진짜 `(1+1/n)^n`을 화면에 옮긴 것이다. n=12에서 e선과 11.7px 벌어지는 것도 "수렴이 실제로 느리다는 사실 그대로"라며 억지로 붙이지 않았다.

**"n=1, n=12를 대입해 검산하라"는 규칙 한 줄이 잘못된 산출물 하나를 막았다.**

**발견 — 뿌리는 내 카탈로그였다**

왜 `spec-analyst`가 틀린 식을 골랐는지 역추적하니, **카탈로그의 행 이름 두 개가 서로 뒤바뀌어 있었다.** 5개 식을 전부 대입해 확인:

| 식 | 수열 거동 | 화면 | 내가 적었던 이름 | 실제 |
|---|---|---|---|---|
| `60 + 80/n` | 증가 | 위로 | ~~위에서 수렴~~ | **아래에서 수렴** |
| `150 - 90/n` | 감소 | 아래로 | ~~아래에서 수렴~~ | **위에서 수렴** |

e는 아래에서 수렴하므로 `spec-analyst`는 "아래에서 한 값에 수렴" 행을 골랐고, 그 행에 잘못된 식이 있었다. **에이전트는 문서를 정확히 따랐고, 문서가 틀렸다.**

게다가 카탈로그 주석은 수학값 방향("아래로 내려와")으로, 본문 설명은 화면 방향으로 서술돼 있어 정반대로 읽혔다.

**하네스 변경**
- `mathuit-tip-graph`: 카탈로그를 **수학값 거동 / 식 / 검산값 / 화면 방향** 4열로 전면 재작성. 모든 값을 실제 대입해 확인
- "위에서/아래에서 수렴은 수학값 기준"임을 명시하고 화면 방향을 별도 열로 분리
- **"카탈로그를 이름만 보고 고르지 마라. 이 표가 틀렸던 적이 있다"** 를 명문화
- 실제 수열값의 1차 변환도 허용됨을 문서화 (`curve`는 JS 식으로 평가되므로 `Math.pow` 사용 가능)
- 부가 규칙 4건: `related` id 확인 방법, `caption`/`oneline`에도 `syms()` 치환 걸림, 라벨 자동 배치(`lines[0]-8`), 스텝 권장(2~4) vs 강제 상한(5) 구분

**핵심 교훈**: 참조 표(카탈로그·룩업 테이블)는 **에이전트가 검증 없이 그대로 믿는다.** 산문 규칙은 문맥으로 걸러지지만 표는 조회 대상이라 의심받지 않는다. 표를 쓸 때는 **모든 행을 실제로 실행해 검산**하고, 하류에는 "표가 틀릴 수 있다"는 경고를 함께 주어야 한다.


---

## 회차 4 — 두 번째 도메인 프로브 (01:46~01:52)

**한 일**: 개선된 `spec-analyst`를 `docs/04_삼각함수_덧셈정리.md`에 **분석 전용**으로 투입 (opus, 94k 토큰, 5분 37초). 파일은 만들지 않았다.

**검증된 것 — 과적합 없음**

회차 1~3에서 고친 판정 체계(4단계·권한 경계·역할 침범 금지·표기 제약)가 성격이 완전히 다른 단원에서 그대로 작동했다. `docs/03` 전용 규칙이 되지 않았다.

**발견 1 — 첫 도메인에서는 가려져 있던 형식의 진짜 한계**

| | `docs/03` (지수·로그) | `docs/04` (삼각함수) |
|---|---|---|
| 개념 수 | 9 | **22** |
| 재사용 / 기존확장 / 신규 | 5 / 0 / 1 | **0 / 0 / 0** |
| 표현불가 | 4 | **7** |
| 팝업이 붙는 개념 | 9 / 9 | **0 / 22** |

`docs/03`은 표현불가 비율이 80%(신규후보 5중 4)였지만 **기존 팁 5개가 살아남아** 모든 개념에 팝업이 붙었다. 그래서 문제가 가려졌다. `docs/04`는 비율이 아니라 **총량이 0**이다 — 기존 22개 팁이 전부 수열·극한 문맥에 묶여 있고 이 단원엔 극한이 하나도 없다.

**두 번째 도메인을 돌리지 않았으면 "팁이 하나도 성립하지 않는 단원"이 존재한다는 걸 몰랐을 것이다.**

**발견 2 — 곡선 렌더러 확장은 답이 아니다**

직관적으로는 "그래프 형식을 함수 곡선까지 확장하면 해결"이라고 생각했는데, 프로브 결과는 반대였다. 표현불가 7개 중 곡선으로 구제되는 건 `trigfunc` 1개뿐이고, 나머지 6개는 **항등식**이라 곡선이 아니라 식 변형이 필요하다. `docs/03`의 표현불가 4개도 곡선으로는 거의 구제되지 않는다.

렌더러 신규 개발 없이 두 단원을 함께 구제하는 유일한 선택지는 **`## graph` 필수 규칙을 완화해 `## steps`만 있는 팁(수식 변형 스토리보드)을 허용**하는 쪽이다. 이건 제품 결정이라 판정하지 않고 옵션별 구제 범위/비용 표를 `_workspace/02_spec-analyst_docs04_probe.md`에 정리해 두었다.

**발견 3 — θ 하드 블로커 (검증 완료)**

`build.py`의 `SYM` 테이블에 그리스 문자가 `alpha`, `beta` **둘뿐**이다. 반각공식 서술에 필요한 θ가 없어 `theta`라는 글자가 화면에 그대로 나온다. 프로브는 θ→A 치환으로 우회했지만 근본 해결이 아니다.

`SYM`에 `('theta','&#952;')` 한 줄을 추가하면 되고, `theta`에는 기존 항목이 부분 문자열로 들어 있지 않아 **위치는 무관하다**(실행으로 확인). 다만 `SYM`이 순서 의존적이라는 점 자체는 사실이다 — `('-oo',...)`가 `('oo',...)`보다 앞에 있는 이유가 그것이다.

**발견 4 — `sin`/`cos` 본문 표기 (심각도 중, 우회 가능)**

실행으로 확인: `prose('sin A')` → `<span class="v">s</span>in` — `s`만 이탤릭이고 `in`은 평문이라 글자가 따로 논다. `mathify`는 `ROMAN_WORDS`에 `sin`/`cos`/`tan`이 있어 정상이다. 즉 **수식은 문제없고 본문만 깨진다.** 본문을 우리말(사인·코사인)로 쓰면 우회되고, 합→곱 변환은 "두 사인의 합/차"가 오히려 더 잘 구분된다. 서술 불가능한 지점은 없었다.

**부수 발견**: 명세 56행 마크다운 깨짐(`$$`로 열고 `$`로 닫음). 퀴즈 후보 3문항(기본 방침대로 제외).

**하네스 변경**
- `spec-analyst`: **표기 가능성 사전 점검** 절 신설 — `SYM` 테이블 직접 대조, 본문 함수명 우회 가능성, 명세 자체의 오류 보고. 저자 단계에서 발견하면 인벤토리를 다시 짜야 하므로 분해 단계로 앞당겼다
- 오케스트레이터: **"표현불가 전량"** 게이트 신설 — 팁이 하나도 성립하지 않으면 개별 개념 문제가 아니라 형식 문제이므로, 착수 전 사람이 결정해야 한다. 진행하면 전량 재작업이다

**핵심 교훈**: 첫 도메인에서 잘 도는 하네스는 그 도메인에 맞춰 만들었으니 잘 도는 게 당연하다. **성격이 다른 두 번째 소재에 분석 단계만** 돌리면 과적합 여부와 형식의 진짜 한계가 함께 드러난다. 파일을 만들지 않으니 비용도 적다.


---

## 회차 5 — 수학 검증, 그리고 무주공산 발견 (01:52~01:58)

**한 일**: 5개 에이전트 중 유일하게 미검증이던 `math-reviewer`를 c13·c15·c16·`euler`에 투입 (opus, 68k 토큰, 3분 24초). 이로써 **에이전트 5개 전부 실전 검증 완료.**

**검증된 것 — build.py가 절대 못 잡는 오류를 잡았다**

판정: c13 통과 · **c15 수정필요** · **c16 수정필요** · euler 통과

1. **c16 유도 서술 오류 (중대).** 본문이 *"`(e^x)'` 공식을 **그대로** 쓰면 `ln a`가 곱해진다"*고 썼는데 자기모순이다. `a^x = e^{x ln a}`에서 `ln a`는 **지수를 미분한 값**(연쇄법칙)이지 `(e^x)' = e^x`를 그대로 적용해 나오는 게 아니다. 학생이 `ln a`의 출처를 재구성할 수 없다.
2. **c15 유일성 문장이 문자 그대로 거짓.** *"미분해도 자기 자신이 되는 함수는 밑이 e일 때뿐"* — `y = 2e^x`도 `y = 0`도 `y' = y`를 만족한다(일반해 `y = Ce^x`). 범위를 지수함수로 한정해야 참이 된다.
3. **`euler.md` curve 독립 재검증 통과.** n=1 → y=140.0, n=12 → y=71.71, 극한 y=59.98 ≈ 보조선 60. 방향 반전 오류 없음.

공식과 조건은 둘 다 맞았다. **틀린 것은 유도를 한 문장으로 압축한 대목**이다. 형식 검사로는 절대 안 걸린다.

**발견 — 축을 나눈 대가로 생긴 무주공산 3개**

`math-reviewer`가 자기 판정 밖이라며 따로 기록해 올린 것들:

| 항목 | 왜 아무도 안 보나 |
|---|---|
| 제목이 인벤토리와 일치하는가 | 수학 오류 아님 · 빌드 통과함 |
| **계획된 개념이 실제로 생성됐는가** | 생성된 파일만 검토 대상이었다 |
| 스텝이 요소를 하나씩 켜는가 | 연출 품질 — 어느 축도 아니다 |

두 번째가 실질적 피해를 냈다. c15의 근거가 c14인데 c14는 승인 대기로 생성되지 않아 **순환 서술**이 됐다. 빌드는 order 공백조차 말해주지 않고, 내용 검토자는 생성된 파일만 본다. `math-reviewer`의 지적이 정확하다 — *"c15 문장만 고쳐서는 해결되지 않는다."*

**하네스 변경**
- `math-reviewer`: **압축된 유도 문장** 점검 축 신설 — "이 설명대로 따라가면 저 결론이 나오는가, 중간에 몰래 다른 규칙이 쓰이지 않았는가"
- `math-reviewer`: **근거가 앱 안에 있는지** 확인 (인벤토리 대조)
- `math-reviewer`: **엄밀성 경계선** 명문화 — *문자 그대로 거짓이면 수정필요, 증명 생략은 통과.* 이 선이 없으면 검토자마다 판정이 갈린다
- `mathuit-build-gate`: **"빌드가 강제하지 않는 규약도 대조하라"** 절 신설. 인벤토리 대 생성물 누락 대조를 스크립트로, 차단이 아니라 보고로

**콘텐츠 수정**: c16 유도 서술 재작성(지수가 x의 상수배라 그 상수가 앞으로 빠져나온다는 실제 메커니즘 서술), c15 유일성 범위 한정. build.py 통과.

**미해결**: c15의 c14 의존은 c14가 승인 대기라 고칠 수 없다. 문장 다듬기로는 해결 안 되는 종류다.

**핵심 교훈**: 검증 축을 나누는 것은 옳지만 **대가가 있다.** 나눈 뒤 "이 항목은 누가 보는가"를 항목별로 훑어 답이 없는 것을 찾아내야 한다. 특히 **계획 대비 누락 대조**는 명시적으로 배정하라 — 산출물만 검토하는 체계는 "없는 것"을 구조적으로 못 본다.


---

## 회차 6 — 부분 재실행 경로 (02:34~02:45)

**한 일**: 점검 중 결함을 발견하고 고친 뒤, 그 수정이 실제로 작동하는지 시험했다.

**발견 — 에이전트 5개 전부 재호출 지침 누락**

오케스트레이터 Phase 0에는 "`_workspace/` 있음 + 부분 수정 요청 → 부분 재실행"이 있는데, **정작 에이전트는 재호출됐을 때 어떻게 행동할지 몰랐다.** Harness 프레임워크 Phase 5-5가 명시적으로 요구하는 항목("에이전트 정의에 재호출 지침 포함")인데 회차 0에서 빠뜨렸다.

경로는 만들어 놓고 그 경로를 걸어갈 사람에게 안내를 안 준 상태였다.

**하네스 변경 — 공통 원칙 + 역할별 지침**

공통: **전면 재작성 금지, 지적된 부분만.** 이미 통과 판정을 받은 부분까지 바뀌면 검토가 통째로 무효가 된다 — 검토자는 자기가 본 문장이 그대로 있다고 가정한다.

| 에이전트 | 역할별 핵심 |
|---|---|
| `spec-analyst` | 인벤토리를 다시 짜면 승인받은 판정까지 바뀌어 **사람이 한 결정이 조용히 사라진다.** id·order 고정 |
| `concept-author` | 리뷰 파일에서 자기 담당만 찾아 그 문장만. 새로 쓰지 말고 편집 |
| `tip-author` | **검증 통과한 `curve`는 건드리지 마라** — 검산이 끝난 식을 검증되지 않은 식으로 바꾸는 셈 |
| `math-reviewer` | 같은 문장이 두 번 지적되면 저자의 실수가 아니라 **규칙이 모호하다는 신호** |
| `build-gate` | 재실행이 정상 동작. 새로 드러난 에러는 저자의 새 실수가 아니라 **앞 에러에 가려져 있던 것** |

**검증 — 지침이 실제로 작동했다**

`euler.md`의 스텝 연출만 고치도록 재호출했다. 결과 diff는 **정확히 한 줄 추가**였고 `curve`를 포함한 나머지 필드는 전부 그대로였다. 빌드 데이터에서 점등 순서가 `(0,0) → (0,0) → (1,0) → (1,1)`로 단계화된 것도 확인했다.

에이전트의 자기 보고가 특히 값졌다:

> 지침이 없었다면 파일 전체를 다시 쓰려 했을 가능성이 높습니다. `curve`가 스킬 카탈로그에 없는 형태라 "표준형으로 정리하자"는 판단을 했을 법하고, 그 순간 n=12에서 e와 눈에 띄게 떨어져 있다는 사실(스킬이 "그 간격 자체가 사실"이라 명시한 부분)이 그래프에서 지워졌을 겁니다. 지적이 하나뿐인데도 저자 에이전트는 통째로 다시 만드는 쪽이 일관성 있다고 느끼는 경향이 있습니다.

**추가 보완**: 에이전트 제안대로 `tip-author` 지침에 "**컷을 늘려 해결해도 된다**"를 추가했다. 상한 5를 강조한 문면 때문에 3컷을 유지한 채 요소를 몰아넣거나 라벨을 빼는 쪽으로 오해할 여지가 있었다.

**핵심 교훈**: 워크플로우에 **경로를 만들었으면 그 경로를 걸어갈 에이전트에게도 안내를 줘야 한다.** 오케스트레이터에 분기를 적는 것과 에이전트가 그 분기에서 어떻게 행동할지 아는 것은 다른 문제다. 그리고 저자 에이전트의 기본 성향은 **전면 재작성**이다 — 명시적으로 막지 않으면 검증된 산출물이 조용히 사라진다.


---

## 회차 7 — 규약 대조가 무주공산을 실제로 잡았다 (02:50~03:10)

**한 일**: 회차 5에서 `build-gate`에 새로 넣은 "빌드가 강제하지 않는 규약 대조"를 실전 투입 (opus, 78k 토큰, 4분 2초, 도구 21회).

**검증된 것 — 설계 의도대로 작동**

`build.py`는 **통과**했다(팁 22, 개념 15, exit 0, 링크 미해결 0). 규약 대조가 없었으면 여기서 끝났을 것이다. 그런데 대조가 **빌드가 절대 못 잡는 문제 2건**을 찾아냈다.

**발견 1 — 근거 사슬이 끊긴 개념 2건**

| 개념 | 문제 |
|---|---|
| `c15` | 본문이 *"남은 극한이 1이 되기 때문이다"*라고 쓰는데, 그 극한(`lim_{h->0}(e^h-1)/h = 1`)을 다루는 **c14가 보류**됐다. 왜 1인지 답할 화면이 앱에 없다 |
| `c16` | 본문의 "자연로그"를 설명하는 화면이 **앱 전체에 없다** |

첫 번째의 진단이 정확하다 — *"보류 단위를 개념 하나가 아니라 **근거 사슬**로 잡았다면 c15도 함께 보류됐어야 한다."*

**발견 2 — 각각은 옳은 결정 둘이 겹쳐 만든 구멍**

c16의 "자연로그" 건이 특히 새롭다. 팁 `naturallog`는 표현불가로 미생성(에스컬레이션 4 — 옳은 결정), 자연로그를 다루는 c17·c18은 sec 미승인으로 보류(에스컬레이션 5 — 역시 옳은 결정). **둘 다 맞는데 겹치면서 그 용어를 설명하는 화면이 하나도 안 남았다.**

각 결정 시점에는 보이지 않는다. 다른 결정을 아직 모르니까.

**발견 3 — 내 규약 대조 절의 결함 4건 (전부 재확인함)**

| 지적 | 확인 |
|---|---|
| 의존 방향을 찾는 **실행 명령이 없다** (지시만 있음). c15→c14 의존은 `[[id]]`가 아니라 **평문에 숨어 있어 grep으로 안 잡힘** | 사실 |
| 중첩 사각지대를 다루는 항목이 없음 | 사실 |
| 스텝 점등 규약이 **신호가 아니라 노이즈** — 기존 22개 중 **14개(63%)가 위반**이라 매번 같은 14줄 | 실측 확인 |
| `comm` 명령 과잉 매칭 — 인벤토리 본문 언급까지 긁어 `c12`가 계획 목록에 섞임 | 실측 확인 |

**부수**: `title` 불일치 2건은 **저자가 옳고 인벤토리가 틀렸다**고 정확히 판정했다(front matter는 변환을 타지 않아 제목에 수식을 넣으면 안 된다). 판정 밖 항목을 판정 밖이라고 정확히 처리했다.

**하네스 변경**
- `mathuit-build-gate`: 규약 대조 절 **전면 개정** — (1) 범위를 `git diff`로 신규·변경 파일에 한정, (2) `comm` 명령을 "## 개념 목록" 표 행에서만 뽑도록 정교화하고 역방향은 보지 않게, (3) **근거 사슬 추적** 절차 신설(인벤토리 수식 열 대 본문 인용 대조), (4) **중첩 사각지대** 점검 신설
- 오케스트레이터: **"보류 단위는 근거 사슬"** 규칙 신설

개정된 명령을 실제로 돌려 확인: 계획 9개 중 보류 6개만 정확히 추출(`c12` 오염 제거).

**핵심 교훈 둘.**

1. **검증 절차에는 지시가 아니라 실행 명령을 써라.** "의존을 확인하라"만 적으면 방법이 없어 눈대중이 된다. 그리고 그 명령을 **직접 돌려보고** 넣어라 — 내 `comm` 명령은 돌려보지 않아 과잉 매칭이 있었다.
2. **매번 나오는 경고는 곧 아무도 안 읽는다.** 기존 자산이 규약을 위반하고 있으면 그 점검은 노이즈다. 범위를 신규·변경분으로 한정하고, 기존 위반은 별도 정리 과제로 분리하라.


---

## 회차 8 — 하네스 자체 감사, 그리고 앱 버그 발견 (03:40~04:15)

**한 일**: 7회 개정이 누적된 하네스에 모순·낡은 서술이 남았는지 감사 (opus, 175k 토큰, 8분 18초, 도구 26회). 콘텐츠가 아니라 **하네스 자체**를 대상으로 했다.

**결과: 높음 6 · 중간 8 · 낮음 8**

**발견 1 — 정정한 버그가 부활할 뻔했다 (높음 1·2)**

`mathuit-content-format` 첫머리에 *"`_README.md`와 충돌하면 그쪽이 우선"*이라고 써 놨는데, **`_README.md`에는 회차 2·3에서 고친 오류가 그대로 남아 있다** — 뒤바뀐 curve 행 이름, "`lim`/`log` 자동 로만체", 본문/수식 구분 없는 기호표.

스킬을 고쳐 놓고 스킬 스스로 틀린 문서에 우선권을 준 셈이다. **우선순위 규칙이 정정을 무효화한다.** `_README`는 사람 편집자용 안내서이고, 충돌 시 스킬이 우선하도록 뒤집었다.

**발견 2 — 라이브 앱 버그 2건 (별도 보고서: `APP-BUGS.md`)**

감사 범위를 넘어선 발견이다. **스프린트가 만든 게 아니라 기존 콘텐츠에 원래 있던 것**이고, 둘 다 `build.py`가 **exit 0**을 낸다.

| 버그 | 화면에 보이는 것 | 왜 빌드가 못 잡나 |
|---|---|---|
| 팁 링크 사망 (개념 03·04) | `[[∑val|급수의 합]]` 대괄호째 노출, 팝업 없음 | `prose()`가 링크 파싱 **전에** `syms()`를 돌려 `sumval`→`&#8721;val`. `data-t`가 생성 자체가 안 되니 "없는 팁" 검사 대상에도 없다 |
| 수식 엔티티 노출 (개념 03·06·07·08·10) | `∑` 대신 `&#8721;`이 글자로 | `mathify()`가 `syms()`가 만든 엔티티를 문자 단위 span으로 분해 |

`index.html:258`이 `innerHTML`로 렌더하는 것까지 확인해 화면 결과를 특정했다. **수정은 하지 않았다** — 렌더링 전반에 영향이 가는 `build.py` 변경이라 사람이 결정할 일이다.

**발견 3 — 나머지 높음 4건**

| 위치 | 문제 |
|---|---|
| `concept-author` | "앞뒤 각 2~4문장"이 정정 전 상태로 잔존 (스킬은 p1 1~2로 고쳐졌는데 에이전트는 그대로) |
| `spec-analyst` | *"`build.py`가 `$$`를 정확히 1개만 허용"* 은 **거짓**. `re.search`가 첫 개만 잡아 2개여도 통과한다 |
| `content-format` | 대조표가 *"`$$` 안: 기호 약어 변환됨"* 이라 안내하는데 **오히려 깨진다** |
| `build-gate` | "첫 에러에서 멈춘다"가 회차 2에서 정정됐는데 에이전트 쪽에 옛 서술 잔존 |

**curve 카탈로그 재검산: 6행 전부 통과.** 회차 3의 수정이 옳았음이 독립적으로 확인됐다. 부수 주장(`Math.pow` 예시값, n=12에서 2.613, 라벨 `lines[0]-8`)도 소스로 전부 확인.

**팀 API 잔재: 없음.** 검출된 6곳은 전부 "이 환경에 없다"는 기록성 서술이다.

**`docs/harness/` 기록 표본 검증: 11건 전부 사실.** 유일한 오차는 "기존 팁 14개(63%) 위반"이 기준에 따라 14~15로 재현되는 것.

**하네스 변경**: 높음 6건 전건 수정, 중간 5건 수정(c12 단원 귀속, 커스텀 에이전트 타입 사용, 죽은 링크 탐지 명령, `related` 검사, `CLAUDE.md` 변경 이력 8행 갱신)

**핵심 교훈 둘.**

1. **우선순위 규칙이 정정을 되살린다.** "충돌하면 X가 우선"이라고 써 두면, X를 고치지 않는 한 내가 고친 내용이 무효가 된다. 외부 문서에 우선권을 줄 때는 **그 문서가 정확한지 먼저 확인**하고, 아니면 우선순위를 뒤집어라.
2. **반복 개정한 하네스는 반드시 감사하라.** 8회차에서 나온 높음 6건 중 4건이 *"앞 회차에서 고쳤는데 다른 파일에 옛 서술이 남은"* 유형이다. 한 사실이 여러 문서에 흩어져 있으면 개정이 한 곳만 닿는다. 그리고 그 감사가 **덤으로 라이브 앱 버그 2건**을 찾아냈다.

---

## 스프린트 마감 정리 (04:45~05:00)

`LESSONS.md`를 재구성했다. 8회차에 걸쳐 유기적으로 자라면서 절 번호가 `6-b`, `8-c` 처럼 엉키고 단계 귀속도 어긋나 있었다 — 다른 세션이 읽을 문서이므로 정리했다.

**5단계 20항목으로 재편:** 시작 전(3) · 설계 중(8) · 실전 투입(4) · 마무리(3) · 반복 개정 후 유지보수(2)

"실전 투입" 단계를 따로 뗀 이유가 있다. 이 스프린트에서 **가장 값진 발견들이 전부 실제로 파일을 만들어 본 뒤에 나왔다.** 설계 리뷰로는 하나도 안 걸렸을 것들이다 — 문서의 사실 오류 5건, 룩업 표 뒤바뀜, 두 번째 도메인의 팁 0개, 무주공산 축, 근거 사슬 끊김. 설계와 실전을 같은 단계로 묶으면 이 구분이 사라진다.

## 최종 상태

| 항목 | 상태 |
|---|---|
| 회차 | 0~8 (9회) |
| 에이전트 | 5개 — 전부 실전 검증 완료 |
| 스킬 | 4개 |
| `build.py` | 통과 (팁 22, 개념 15) |
| 팀 API 잔재 | 없음 |
| 상호 참조 dead link | 없음 |
| 생성 콘텐츠 | 개념 3(c13·c15·c16) + 팁 1(euler) |
| 분석 완료 | `docs/03` 인벤토리, `docs/04` 프로브 |

## 검증하지 않은 것 (의도적)

**오케스트레이터 end-to-end 전체 실행.** 각 단계(Phase 2~5)와 재호출 경로를 개별 검증했고, 전체 실행은 이미 검증된 부분을 다시 돌리며 토큰만 크게 쓴다고 판단했다. 다만 **Phase 간 데이터 인계가 실제로 매끄러운지는 확인되지 않았다** — 개별 호출 시에는 내가 매번 입력을 정리해 넘겼기 때문이다.

## 사람 결정 대기

1. **팁 그래프 형식 확장** — 가장 중요. `docs/04`는 개념 22개 전부 팁 0개다. 곡선 렌더러 확장은 7개 중 1개만 구제하므로 답이 아니고, `## graph` 필수 완화(`## steps`만 있는 팁 허용)가 유일한 저비용 선택지다. 옵션별 표: `_workspace/02_spec-analyst_docs04_probe.md`
2. **라이브 앱 버그 2건** — `docs/harness/APP-BUGS.md`. `build.py` 수정이 필요하고 렌더링 전반에 영향이 간다
3. `indeterminate`·`base` oneline 확장 (c14·c18 착수 조건)
4. 신규 sec 2개 승인 (c17~c21 착수 조건)
5. `SYM`에 `theta` 추가 (`docs/04` 착수 조건)
6. c15의 근거 사슬 — c14 없이 출고된 상태를 유지할지, c15도 함께 보류할지
7. 퀴즈 개념화 범위 (현재 기본값: 만들지 않음)
